# TINAS-ShipDet Requirements
# Laptop (training + search orchestration)
ultralytics>=8.1.0
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
pyyaml>=6.0
psutil>=5.9.0
matplotlib>=3.7.0
opencv-python>=4.8.0

# Optional: desktop GPU power monitoring
nvidia-ml-py3>=7.352.0

# SSH-based Jetson remote profiling
paramiko>=3.0.0

# Jetson (profiling only — install on Jetson Orin Nano)
# ultralytics, torch (JetPack-provided), psutil, pyyaml, numpy
# TensorRT is pre-installed with JetPack
